sitemaps:
   example:
     default: en-gb
     languages:
       en-gb:
         source: /en-gb/query-index.json
         destination: /sitemap.xml
         hreflang: en-gb
         alternate: /en-gb/{path}
       en-us:
         source: /en-us/query-index.json
         destination: /sitemap.xml
         hreflang: en-US
         alternate: /en-us/{path}
